# Worm

==============================================================

git clone https://github.com/Nul1Ro0ts/Worm.git

cd Worm

cd worm 

cargo build --release

# cross-compile example for a target host's arch:

cargo build --release --target x86_64-unknown-linux-musl

==============================================================
